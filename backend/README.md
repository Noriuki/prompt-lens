# Backend – Prompt Lens

Scaffold FastAPI + Clean Architecture. Ver [README na raiz do projeto](../README.md) para visão geral.

**Rodar:** `python run.py` (antes: `pip install -e .`; use um venv).

**Testes:** `pytest` (a partir desta pasta).
